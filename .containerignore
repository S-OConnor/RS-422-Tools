# Build context for the C++ images (Containerfile.build / Containerfile.runtime),
# whose context is the repo root. Keep it to the C++ project only — everything
# else is either unrelated or rebuilt inside the image.
tools/
environments/
docs/
scripts/
.git/
.claude/
build/
**/build/
*.o
*.a
.cache/
